#include <stdio.h>

int main(void){

    int snum; // 변수를 사용하기 위해 변수 선언
    int credits;

    snum = 2019112; //변수에 값을 대입
    credits = 18;

    printf("Student Number : %d\n", snum);
    printf("credit: %d\n", credits);

    return 0;
}